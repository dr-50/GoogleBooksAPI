const typeDefs = require('./typeDefs')
const resolvers = require('./resolvers')

module.exports = { typeDefs, resolvers }

//stopped at resolvers.js define the query and mutation functionality to work with the Mongoose models